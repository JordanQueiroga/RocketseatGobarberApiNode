const { resolve } = require('path');

module.exports = {
  config: resolve(__dirname, "src", "config", "databese.js"),
  'models-path': resolve(__dirname, "src", "app", "models"),
  'migrations-path': resolve(__dirname, "src", "databese", "migrations"),
  'seeders-path': resolve(__dirname, "src", "databese", "seeds"),
};
